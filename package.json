{
  "name": "bengsfort.dev",
  "packageManager": "yarn@3.2.0",
  "private": true,
  "workspaces": [
    "packages/*",
    "tools/*"
  ],
  "devDependencies": {
    "@bengsfort.dev/eslint-config": "workspace:^",
    "@types/eslint": "^8",
    "eslint": "^8.33.0"
  },
  "scripts": {
    "lint": "yarn workspaces foreach -pt run lint"
  },
  "engines": {
    "node": ">=16.3.0"
  }
}

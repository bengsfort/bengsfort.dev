{
  "name": "bengsfort.dev",
  "packageManager": "yarn@3.2.0",
  "private": true,
  "type": "module",
  "workspaces": [
    "packages/*",
    "tools/*"
  ],
  "devDependencies": {
    "@bengsfort.dev/eslint-config": "workspace:^",
    "@types/eslint": "^8.21.0",
    "eslint": "^8.33.0",
    "typescript": "^4.9.5"
  },
  "scripts": {
    "lint": "yarn workspaces foreach -pt run lint",
    "dev": "yarn workspace '@bengsfort.dev/server' run dev"
  },
  "engines": {
    "node": ">=16.3.0"
  }
}
